gigaam==0.1.0
silero-vad>=5.1
# Пины обязательны: GigaAM 0.1.0 совместим только с torch/torchaudio <2.9,
# а silero-vad тянет их без верхнего pin. Без пинов pip возьмёт latest (2.11+),
# что заставит silero требовать torchcodec и сломает GigaAM.
torch>=2.5,<2.9
torchaudio>=2.5,<2.9
